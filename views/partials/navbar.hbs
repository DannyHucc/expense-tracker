{{!-- views/partials/navbar.hbs --}}
<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <i class="fa-solid fa-circle-dollar-to-slot"></i>
            {{#if isAuthenticated}}{{user.name}}'s{{else}}My{{/if}}
            Expense Tracker</a>
        {{#if isAuthenticated}}
        <div class="d-flex">
            <a class="btn btn-outline-primary me-1" href="/records/new" role="button">ADD RECORD</a>
            <a class="btn btn-outline-primary" href="/users/logout" role="button">LOGOUT</a>
        </div>
        {{else}}
        <a class="btn btn-outline-primary" href="/users/login" role="button">LOGIN</a>
        {{/if}}
    </div>
</nav>